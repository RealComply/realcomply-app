// Bump this when the item set or wording in nsw-sales.ts changes meaningfully.
// Stamped on every export so a record is traceable to the exact ruleset in
// force when it was generated, per the "immutable audit trail stamped with the
// ruleset version" principle in the website IA doc.
//
// Lives in its own file since 23 Aug 2026 so the summary page and the PDF route
// cannot drift apart. A record whose stamp disagrees with the rules that
// produced it is worse than one with no stamp at all.
export const RULESET_VERSION = "NSW Sales Ruleset 2026.2";
